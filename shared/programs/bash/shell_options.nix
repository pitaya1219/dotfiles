[
  "histappend"   # Append to history file instead of overwriting it when shell exits
  "checkwinsize" # Check window size after each command and update LINES/COLUMNS variables
  "extglob"      # Enable extended pattern matching (like ?(pattern), *(pattern))
  "globstar"     # Enable ** recursive glob patterns (e.g., **/*.txt matches all .txt files recursively)
  "checkjobs"    # Display job status before exiting shell if jobs are running
  "cmdhist"      # Combine multi-line commands into a single history entry
]